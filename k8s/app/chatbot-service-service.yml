apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: chatbot
  labels:
    app: chatbot-service
spec:
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
  selector:
    app: chatbot-service
  type: ClusterIP
